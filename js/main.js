const buyTypes=['⚡ CONWAY BUY','SSL2 ↑','GOLD BUY'];const sellTypes=['⚡ DOOM SELL','SSL2 ↓','CONWAY DIED'];const syms=['BTCUSDT','XAUUSD','EURUSD','GBPUSD','ETHUSDT'];const sessions=['LONDON','NEW YORK','ASIA'];let simRunning=false;setTimeout(()=>{const feed=document.querySelector('.feed-box');const firstRow=feed.querySelector('.sig-row');const now=new Date();const t=`${now.getHours().toString().padStart(2,'0')}:${now.getMinutes().toString().padStart(2,'0')}:${now.getSeconds().toString().padStart(2,'0')}`;const isBuy=Math.random()>0.5;const type=(isBuy?buyTypes:sellTypes)[Math.floor(Math.random()*3)];const sym=syms[Math.floor(Math.random()*syms.length)];const sess=sessions[Math.floor(Math.random()*sessions.length)];const newRow=document.createElement('div');newRow.className='sig-row highlight new';newRow.innerHTML=`<span class="st">${t}</span><span class="stype ${isBuy?'buy':'sell'}">${type}</span><span class="ssym">${sym}</span><span class="sprice">—</span><span class="ssess">${sess}</span><span class="sdesc">Live simulated signal</span><span class="spnl ${isBuy?'up':'dn'}">${isBuy?'+':'-'}${(Math.random()*2+0.5).toFixed(1)}%</span>`;firstRow.parentNode.insertBefore(newRow,firstRow);setTimeout(()=>newRow.classList.remove('highlight'),3000);},8000);const allPips=document.querySelectorAll('.cw-card .pip.on');setInterval(()=>{if(allPips.length){const rp=allPips[Math.floor(Math.random()*allPips.length)];rp.style.opacity='0.35';setTimeout(()=>{rp.style.opacity='1';},400);}},2500);
